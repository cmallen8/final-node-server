/*import mongoose from "mongoose";
const followsShowSchema = new mongoose.Schema(
    {
        userId: String,
        show: {type: mongoose.Schema.Types.ObjectId, ref: "shows"}
    },
    {collection: "followsShow"}
)
export default followsShowSchema;
*/
